// Governed by .rules v1.0
import { useId, type InputHTMLAttributes, type ReactNode } from 'react';
import { cn } from '@/lib/utils';
export interface InputProps extends InputHTMLAttributes<HTMLInputElement> { label: string; error?: string; }
export function Input({ label, error, id, className, ...props }: InputProps): ReactNode { const generatedId = useId().replace(/:/g, ''); const inputId = id ?? label.toLowerCase().replace(/\s+/g, '-') + '-' + generatedId; return <label className="block text-xs uppercase tracking-[0.15em] text-text-secondary" htmlFor={inputId}><span>{label}</span><input id={inputId} className={cn('mt-2 h-12 w-full border border-border-subtle bg-background-input px-4 text-text-primary transition placeholder:text-text-muted focus:border-border-strong focus:bg-background-overlay', className)} aria-invalid={Boolean(error)} aria-describedby={error ? inputId + '-error' : undefined} {...props} />{error ? <span id={inputId + '-error'} className="mt-2 block text-xs text-danger" aria-live="polite">{error}</span> : null}</label>; }
